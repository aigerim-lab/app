<!DOCTYPE html>
<html lang="en">
<head>
  <title>Task List</title>
</head>
<body>
  <h1>Список задач</h1>

  <% if (tasks.length > 0) { %>
    <ul>
      <% tasks.forEach(task => { %>
        <li>
          <strong><%= task.title %></strong> - <%= task.status %>
          <p><%= task.description %></p>
          <small>Создано: <%= task.createdAt.toLocaleDateString() %></small>
        </li>
      <% }) %>
    </ul>
  <% } else { %>
    <p>Задач пока нет.</p>
  <% } %>
</body>
</html>
